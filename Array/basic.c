// collection of the same type of data
// address are have continuos memory 
#include<stdio.h>
int main(){
    int arr[5]={2,4,6,8,1};
    //  index -> 0 to n-1 where n is  the size of array
    printf("%d\n",arr[2]);
    // updatation of array
    arr[4]=100;
    printf("%d",arr[4]); 
    
}